# Step 5 Authentication

Copy the backend/app contents into your existing backend.

Install:
pip install python-jose passlib[bcrypt]

Available endpoints:
POST /auth/login
POST /auth/register

Default demo credentials:
username: admin
password: admin123
